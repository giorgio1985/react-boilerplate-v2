/*
 * Team Messages
 *
 * This contains all the text for the Team container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.container.Team';

export default defineMessages({
  team: {
    id: `${scope}.team`,
    defaultMessage: 'This is the Team container!',
  },
  underTitle: {
    id: `${scope}.underTitle`,
    defaultMessage: 'This is the Team container!',
  },
});
