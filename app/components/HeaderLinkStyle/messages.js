/*
 * HeaderLinkStyle Messages
 *
 * This contains all the text for the HeaderLinkStyle component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderLinkStyle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderLinkStyle component!',
  },
});
