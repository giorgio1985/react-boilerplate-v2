/*
 * Iframe Messages
 *
 * This contains all the text for the Iframe component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Iframe';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Iframe component!',
  },
});
